{
  "private": true,
  "scripts": {
    "start": "node index.js",
    "lint": "eslint .",
    "test": "npm run lint",
    "deploy": "now --public -e NODE_ENV=production --token $NOW_TOKEN --npm",
    "alias": "now alias --token=$NOW_TOKEN"
  },
  "dependencies": {
    "express": "^4.16.3"
  },
  "devDependencies": {
    "eslint": "^4.8.0",
    "eslint-plugin-react": "^7.7.0",
    "eslint-watch": "^3.1.3",
    "now": "^10.2.1"
  },
  "now": {
    "name": "test-traivs-ci",
    "alias": "test-travis-ci"
  }
}
